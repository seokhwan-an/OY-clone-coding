spring:
  application:
    name = clonecoding

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:0809/oy
    username: root
    password: mysql

  jpa:
    hibernate:
      ddl-auto: update
